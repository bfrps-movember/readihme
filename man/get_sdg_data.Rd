% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-data.R
\name{get_sdg_data}
\alias{get_sdg_data}
\title{Get SDG Data for Multiple Countries and Years}
\usage{
get_sdg_data(
  indicator_ids,
  location_ids,
  years,
  sex_id = NULL,
  age_group_id = NULL,
  scenario = NULL
)
}
\arguments{
\item{indicator_ids}{Vector of indicator IDs}

\item{location_ids}{Vector of location IDs (countries)}

\item{years}{Vector of years}

\item{sex_id}{Optional sex ID filter}

\item{age_group_id}{Optional age group ID filter}

\item{scenario}{Optional scenario filter}
}
\value{
Combined data frame with results
}
\description{
This is a convenience function that retrieves data for multiple indicators,
countries, and years in a single call. It handles the API requests internally
and combines the results.
}
\examples{
\dontshow{if (interactive() && curl::has_internet()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
get_sdg_data(indicator_ids = c(1, 2), location_ids = c(102, 6), years = c(2020, 2021))
\dontshow{\}) # examplesIf}
}
