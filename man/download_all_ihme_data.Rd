% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-data.R
\name{download_all_ihme_data}
\alias{download_all_ihme_data}
\title{Download All IHME Data}
\usage{
download_all_ihme_data(
  file = "ihme_data.rds",
  force = FALSE,
  years = 1990:2030,
  batch_size = 3,
  delay_between_batches = 2,
  include_projections = TRUE,
  location_batch_size = 50,
  delay_between_location_batches = 0.5
)
}
\arguments{
\item{file}{Path to save data (default: "ihme_data.rds")}

\item{force}{Force redownload, even if file exists and is current (default: FALSE)}

\item{years}{Vector of years to download data for (default: 1990:2030)}

\item{batch_size}{Number of indicators to process per batch (default: 3)}

\item{delay_between_batches}{Delay in seconds between indicator batches (default: 2)}

\item{include_projections}{Include projection years (default: TRUE)}

\item{location_batch_size}{Number of locations to process per batch (default: 50)}

\item{delay_between_location_batches}{Delay in seconds between location batches (default: 0.5)}
}
\value{
List containing all IHME data
}
\description{
Downloads all available metadata AND SDG indicator data from the IHME API.
}
\examples{
\dontshow{if (interactive() && curl::has_internet()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
all_data <- download_all_ihme_data()
recent_data <- download_all_ihme_data(years = c(2020, 2025), file = "ihme_recent.rds")
\dontshow{\}) # examplesIf}
}
